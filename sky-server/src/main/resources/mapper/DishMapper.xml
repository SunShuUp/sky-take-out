<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="add" useGeneratedKeys="true" keyProperty="id"  >
        insert into dish (name,category_id,image,description,status,create_time,update_time,create_user,update_user)
        values (#{name},#{categoryId},#{image},#{description},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
    <update id="update">
        update dish
        <set>
            <if test="name!=null and name !=''">
                name =#{name},
            </if>
            <if test="categoryId!=null">
                category_id=#{categoryId},
            </if>
            <if test="price!=null">
                price=#{price},
            </if>
            <if test="image!=null">
                image=#{image},
            </if>
            <if test="description!=null">
                description=#{description},
            </if>
            <if test="createTime!=null">
                create_time=#{createTime},
            </if>
            <if test="status!=null">
                status=#{status},
            </if>
            <if test="updateTime!=null">
                update_time=#{updateTime},
            </if>
            <if test="createUser!=null">
                create_user=#{createUser},
            </if>
            <if test="updateUser!=null">
                update_user=#{updateUser},
            </if>
        </set>
        <where>
            <if test="id!=null">
                id=#{id};
            </if>
        </where>
    </update>

    <select id="page" resultType="com.sky.vo.DishVO">
        select d.*,c.name category_name
        from dish d,category c
        <where>
            d.category_id=c.id
            <if test="categoryId!=null">
               and   d.category_id=#{categoryId}
            </if>
            <if test="name!=null and name !=''">
                and d.name like concat('%',#{name},'%')
            </if>
            <if test="status!=null">
                and d.status=#{status}
            </if>
        </where>
        order by d.create_time desc
    </select>
    <select id="getDishById" resultType="com.sky.entity.Dish">
        select * from dish
        <where>
            <if test="id!=null">
                and id=#{id}
            </if>
        </where>
    </select>
    <select id="query" resultType="com.sky.vo.DishVO">
        select d.*,c.name categoryName
        from  dish d,category c
        <where>
            d.category_id =c.id
            and c.type=1
            <if test="categoryId!=null">
                and c.id=#{categoryId}
            </if>
        </where>
    </select>
</mapper>
